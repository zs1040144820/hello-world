git config --global user.name ""
git config --global user.email ""
//配置
git config -l
//查看配置
git init
//初始化

git remote add origin https://github.com/zs1040144820/hello-world.git
//连接
git status
//查看文件
//
git add *
git commit -m first_commit
//提交
git push -u origin master
//放上
